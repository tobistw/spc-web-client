'use strict';

describe('SPC Web Client', function() {

    //it('should redirect other routes to login.html', function() {
    //    browser.get('/');
    //    browser.getLocationAbsUrl().then(function(url) {
    //        expect(url).toEqual('/login');
    //    });
    //});
});
